version: '3.8'

services:
  api:
    image: 13angs/task-scheduler:latest
    build:
      context: ../../api
      dockerfile: ./Dockerfile
    ports:
      - 5000:5000
    
    networks:
      - mysql-network
    
    environment:
      - "ConnectionStrings__Mysql=Data Source=mysql;Port=3306;User ID=root;password=password;Database=QRTZ_DB;"
      - "Scheduler__Secret=${SIG_SECRET}"

networks:
  mysql-network:
    name: mysql-network